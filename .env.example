# Environment Configuration for 5R Associates Backend
# Copy this to .env and fill in your actual values

# Encryption Master Password (used to decrypt Web3Forms access key)
# IMPORTANT: Keep this secret! Don't commit to Git!
ENCRYPTION_PASSWORD=scorpwd123 

# Server Port
PORT=3000

# WhatsApp Business Number (optional, for WhatsApp integration)
WHATSAPP_NUMBER=N/A yet

# Admin API token (used to view decrypted logs via /api/admin/logs)
ADMIN_TOKEN=change_me_admin_token

# Web3Forms will be decrypted from config/keys.encrypted at runtime
# No need to put it here - it's encrypted and stored separately
